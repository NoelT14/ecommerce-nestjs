export enum StockStatus {
  IN_STOCK = 'in_stock',
  OUT_OF_STOCK = 'out_of_stock',
  LOW_STOCK = 'low_stock',
  BACKORDER = 'backorder',//The item is not currently in stock, but orders are still allowed.
}
